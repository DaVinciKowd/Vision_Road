/*
import 'package:flutter/material.dart';
import '../widgets/hazard_detected.dart';

class PopupPreviewPage extends StatelessWidget {
  const PopupPreviewPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.grey.shade300, // helps see opacity & shadow
      body: Center(
        child: HazardDetectedBanner(), // your popup widget
      ),
    );
  }
}
*/

// Hazard Registered
/*
import 'package:flutter/material.dart';
import '../widgets/hazard_registered.dart';

class PopupPreviewPage extends StatelessWidget {
  const PopupPreviewPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.grey.shade300, // helps see opacity & shadow
      body: Center(
        child: HazardRegisteredBanner(), // your popup widget
      ),
    );
  }
}
*/

// Hazard ALert
/*
import 'package:flutter/material.dart';
import '../widgets/hazard_alert.dart';

class PopupPreviewPage extends StatelessWidget {
  const PopupPreviewPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.grey.shade300, // helps see opacity & shadow
      body: Center(
        child: HazardAlertBanner(), // your popup widget
      ),
    );
  }
}
*/

//Hazard Type Banner
import 'package:flutter/material.dart';
import '../widgets/hazard_type.dart';

class PopupPreviewPage extends StatelessWidget {
  const PopupPreviewPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.grey.shade300, // helps see opacity & shadow
      body: Center(
        child: HazardTypeBanner(), // your popup widget
      ),
    );
  }
}

