Expected file path:
assets/models/road_hazard_detector.tflite

Important:
- In-app inference currently supports TFLite models (.tflite).
- If your source model is PyTorch (.pt), convert it to .tflite first.

After adding the file, run:
flutter pub get
flutter run
